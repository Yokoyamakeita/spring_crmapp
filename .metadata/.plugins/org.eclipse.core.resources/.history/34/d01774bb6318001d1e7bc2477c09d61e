<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" 
xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout" 
layout:decorate="~{layout/layout}">

<head>
<title>ユーザ一覧</title>
<link rel="stylesheet" th:href="@{/css/user/list.css}">
</head>
<body>
<div layout:fragment="content">
    <div class="header border-bottom">
        <h1 class="h2">ユーザ一覧画面</h1>
    </div>
    
    <div class="mb-4">
  		<form th:action="@{/user/list}" method="post" id="user-search-form" class="form-inline" th:object="${userListForm}">
    	<div class="col">
      		<label for="userId" class="mr-2">ユーザID</label>
      		<input type="text" class="form-control" th:field="*{userId}">
    	</div>
    	<div class="col">
      		<label class="mr-2">ユーザ名</label>
      		<input type="text" class="form-control" th:field="*{userName}">
    	</div>
    	<button type="submit" class="btn btn-primary">
      	検索
    	</button>
  		</form>
	</div>
    
    <div >
  <table class="table table-striped table-bordered table-hover">
    <thead class="thead-light">
      <tr>
        <th class="th-wisth">ユーザID</th>
        <th class="th-wisth">ユーザ名</th>
        <th class="th-wisth">誕生日</th>
        <th class="th-wisth">年齢</th>
        <th class="th-wisth">性別</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <tr th:each="item:${userList}">
        <td th:text="${item.userId}"></td>
        <td th:text="${item.userName}"></td>
        <td th:text="${#dates.format(item.birthday,'YYYY/MM/dd')}"></td>
        <td th:text="${item.age}"></td>
        <td th:text="${item.gender == 1} ? '男性':'女性'"></td>  
        <td><a class="btn btn-primary" th:href="@{'/user/detail/'+ ${item.userId}}">詳細</a></td>
      </tr>
    </tbody>
  </table>
</div>
</div>
</body>
</html>